## What changed

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] `npm test` passes
- [ ] `npm run build` run locally, and `dist/` committed in this PR if the
      generator or `wall.config.json` changed (`npm run stamp` passes)
- [ ] `node scripts/check-build.js dist` is clean
- [ ] Docs updated where behaviour changed (ARCHITECTURE / API / SETUP) and
      `docs/CHANGELOG.md` has a dated `[Feature]` / `[Fix]` / `[Break]` entry
- [ ] Verified in a browser at `npm run serve` (pan, zoom, fit, a phone size)
- [ ] No em dashes in code, comments, docs or copy
